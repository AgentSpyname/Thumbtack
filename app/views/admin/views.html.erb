<div class="col-lg-12 col-xs-12">
<table class="table">
    <thead>
    <th>Title</th>
    <th>View Count</th>
    <th>Link</th>
    </thead>
    
    <tbody>
          <% @posts.each do |post| %>
        <tr>
            <td><%= post.title %></td>
            <td><%= post.impressions.count %></td>
            <td></td>
        </tr>
        <% end %>

    </tbody>
</table>
</div>