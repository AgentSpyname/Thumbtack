<%= render 'layouts/menubar' %>
<%=  Liquid::Template.parse(@page.layout.custom_layout).
      render('page_content' => yield, 'page_title' => yield(:title), "menu_items" => @menu_list_items
 ).html_safe %>
      
